spring:
  application:
    name: gateway-server
  cloud:
    nacos:
      server-addr: 192.168.142.101:8848 # nacos地址
      config:
        file-extension: yaml # 文件后缀名
        shared-configs: # 共享配置
          - dataId: store-shared-log.yaml # 共享路由配置
          - dataId: store-auth.yaml # 共享权限配置
          - dataId: store-jwt.yaml # 共享密钥配置
